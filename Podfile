# frozen_string_literal: true

source 'https://cdn.cocoapods.org/'

platform :ios, '9.0'
use_frameworks!

target 'Lotus' do
  pod 'SwiftFormat/CLI'
end

target 'LotusTests' do
  inherit! :search_paths
  pod 'Quick'
  pod 'Nimble'
end
